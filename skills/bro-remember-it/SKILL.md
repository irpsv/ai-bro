---
name: bro-remember-it
description: Пополняет существующий memory bank — постоянный слой знаний проекта. Use when the user wants to update project knowledge, specs, features, playbooks, or asks «запомни», «добавь в memory bank», «/bro-remember-it». Не инициализирует банк с нуля.
disable-model-invocation: true
---

# Bro Remember It

Твоя задача — **пополнять** существующий **memory bank**: постоянный слой знаний проекта (контекст, фичи, playbooks).

**НЕ ИНИЦИАЛИЗИРУЙ** банк с нуля — для этого есть скилл инициализации (`/bro-init-memory-bank`).
**НЕ СОЗДАВАЙ** артефакты планирования в `plans/` — для планов используется скилл планирования (`/bro-give-me-plan`).

## ОБЩИЕ ПРАВИЛА

1. **ОБЯЗАТЕЛЬНО** соблюдай [правила диалога](./references/dialog-rules.md)!
2. **ОБЯЗАТЕЛЬНО** соблюдай [структуру memory bank](./references/memory-bank-structure.md) и [правила содержимого](./references/content-rules.md)!
3. **ОБЯЗАТЕЛЬНО** соблюдай и **НЕ нарушай** [порядок работы](#порядок-работы)!
4. Сначала самостоятельно собери контекст из репозитория и существующего банка.
5. Не спрашивай то, что можно установить с высокой уверенностью.
6. Помечай выведенное как выведенное, пока пользователь его не подтвердит.

## ОГРАНИЧЕНИЯ / STOP

1. **ЗАПРЕЩЕНО** создавать memory bank с нуля и запускать init-скрипты.
2. **ЗАПРЕЩЕНО** создавать или изменять файлы в `plans/`.
3. **ЗАПРЕЩЕНО** ссылаться из `features/` на `plans/`.
4. **ЗАПРЕЩЕНО** создавать подкаталоги в `features/` — одна фича, один файл.
5. **ЗАПРЕЩЕНО** придумывать факты о проекте.

## ПОРЯДОК РАБОТЫ

1. **ОПРЕДЕЛИ** путь к memory bank (по умолчанию `memory-bank/` в корне репозитория).
2. **ПРОВЕРЬ**, что `memory-bank/index.md` существует. Если нет — **ОСТАНОВИСЬ**, сообщи что банк не инициализирован, и **ПРЕДЛОЖИ** `/bro-init-memory-bank`.
3. **ПРОЧИТАЙ** [replenish](./workflows/replenish.md) и **СОБЛЮДАЙ** его инструкции.
4. После завершения **ПОКАЖИ** человеку итог: что изменено, что требует уточнения.

## Quality Control

Перед завершением проверь:

- структура не нарушена: `index.md`, `project/`, `features/`, `playbooks/`, `plans/`;
- корневой `index.md` ссылается только на `project/*.md`, `features/index.md`, `playbooks/index.md`;
- фичи — плоские файлы `features/<feature-id>.md`, реестр в `features/index.md`;
- playbooks — плоские файлы `playbooks/<slug>.md`, реестр в `playbooks/index.md`;
- нет дублей и выдуманных фактов;
- `plans/` не затронут.
