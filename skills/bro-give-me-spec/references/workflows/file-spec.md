# File Spec

Workflow для спецификации как файлового артефакта.

## ОГРАНИЧЕНИЯ / STOP

- **ЗАПРЕЩЕНО** выбирать путь размещения файла спецификации самому.
- **ЗАПРЕЩЕНО** записывать файл спецификации, пока человек не назвал однозначный путь. Путь, названный в текущем запросе, уже считается ответом — не спрашивай его повторно.
- Если файл по заданному пути уже существует — **ЗАПРЕЩЕНО** обновлять его без явного одобрения на обновление.

## ПОРЯДОК РАБОТЫ

1. **ОПРЕДЕЛИ** путь файла спецификации. Если человек уже назвал однозначный полный путь в запросе — используй его и **не спрашивай** повторно. Если пути нет или он двусмысленный — **СПРОСИ** полный путь. Путь **не** выбирай сам. Если файл по заданному пути уже есть — прочитай его, **СООБЩИ** человеку и получи явное одобрение на обновление.
2. **СОЗДАЙ** или **ОБНОВИ** файл спецификации по шаблону и заданному пути. Не жди отдельного апрува содержания перед записью.

## Quality Control

Перед завершением проверь:

- артефакт находится по заданному человеком пути;
- путь назвал человек (в запросе или в ответ на вопрос) до первой записи; если файл уже существовал — получено одобрение на обновление;
- в артефакте есть все пять обязательных разделов и нет лишних, кроме допустимого `Принятые решения`;
- нет шагов, этапов и способа реализации;
- каждый изменяемый сценарий покрыт критерием приемки;
- каждый критерий содержит проверяемое доказательство;
- ревью выполнено по правилам (или явно пропущено человеком);
- при `NEEDS_WORK` файл обновлён, при `BLOCKED` задан вопрос человеку;
- `/bro-do-it` предложен только после `PASS` или явного пропуска ревью.
